[package]
name = "libbsd-sys"
version = "0.1.0"
edition = "2021"
description = "Raw FFI bindings to libbsd"
license = "MIT OR Apache-2.0 OR ISC"
links = "bsd"

[dependencies]
libc = "0.2"

[build-dependencies]
pkg-config = "0.3"
